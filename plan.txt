# Redis-GunYu 改造实施计划（重排版）

## 一、改造目标

1. 先落地 **元数据 Redis Sentinel + Checkpoint 写入链路**，作为后续所有阶段的前置依赖。
2. 在不影响主数据链路（源端 -> 本地落地 -> 目标端）的前提下，完成 Input/Output 解耦。
3. 再实现 Input/Output 高可用与故障切换。
4. 最后实现按 Slot（16384）的 checkpoint，支持异构分片集群双向同步。
5. 增加前端 Web 管理平台，支持同步程序安装配置、多实例统一管理与已有实例纳管。

---

## 二、总体原则

- Checkpoint 采用双边界：`input_cp` 在 **meta.redis（Sentinel 托管）**，`output_cp` 在 **目标端 Redis**。
- 两类 checkpoint 严格命名隔离，禁止混用/复用 key。
- 本地落地（channel）继续作为数据缓冲与恢复介质。
- 阶段之间必须有明确依赖，不跳阶段。

---

## 三、里程碑顺序（重新规划）

### 阶段0：元数据 Redis Sentinel 基座（必须先做）

**目标**：先把 checkpoint 写入元数据 Redis Sentinel 路径打通，形成稳定依赖。

**工作量**：4-6天  
**风险**：中-高

#### [x] 任务0.1：补齐 Sentinel 客户端能力
- **文件**：`pkg/redis/client/`、`config/config.go`、`config/var.go`
- **内容**：
  - 支持 `RedisTypeSentinel` 连接
  - 支持 masterName、sentinel auth、切主重连
  - 输出明确的连接/切主日志与指标

#### [x] 任务0.2：定义元数据 Redis 配置
- **文件**：`config/*.yaml`、`config/config.go`
- **内容**：
  - 新增 `meta.redis` 配置（与 input/output redis 解耦）
  - 支持 sentinel 地址列表、masterName、认证参数

#### [x] 任务0.3：Checkpoint 写入链路先打通
- **文件**：`syncer/output.go`、`pkg/redis/checkpoint/checkpoint.go`
- **内容**：
  - Input checkpoint 写入 `meta.redis`
  - Output 回放成功后 checkpoint 写入目标端 Redis
  - 增加重试、超时、错误分类（可重试/不可重试）

#### [x] 任务0.4：Checkpoint 读取链路打通
- **文件**：`syncer/input.go`、`syncer/output.go`
- **内容**：
  - Input 启动时从 `meta.redis` 读取 checkpoint
  - Output 启动时从目标端 checkpoint 获取起点

#### [ ] 任务0.5：验收测试
- [x] Sentinel 主从切换测试
- [x] 进程重启恢复测试
- [ ] 断网抖动下 checkpoint 连续性测试

**完成标准**：
- [x] checkpoint 双边界生效：`input_cp` 落 `meta.redis`，`output_cp` 落目标端
- [x] Sentinel 切主后 30s 内自动恢复 checkpoint 读写
- [x] 进程重启后可从 checkpoint 正常续传

---

### 阶段1：Input/Output 解耦（依赖阶段0）

**目标**：解耦运行时依赖，Input 和 Output 互不直接调用。

**工作量**：6-9天  
**风险**：中

#### [x] 任务1.1：Input 仅负责“读取源端 -> 写本地落地”
- **文件**：`syncer/input.go`
- **内容**：
  - 保留 channel 写入逻辑
  - 去除 Output 直接调用路径
  - 启动/续传点完全依赖 `meta.redis checkpoint + channel`

#### [x] 任务1.2：Output 仅负责“读本地落地 -> 写目标端”
- **文件**：`syncer/output.go`、`syncer/output_runner.go`
- **内容**：
  - 起点读取来自目标端checkpoint，并与本地落地文件可读区间比较校正
  - 回放成功后更新目标端checkpoint

#### [x] 任务1.3：重构 syncer 主流程
- **文件**：`syncer/syncer.go`
- **内容**：
  - Input、Output 独立 goroutine/runner
  - 错误传播与退出语义拆分
  - output 异常默认隔离重试；命中红线时受控联动退出

#### [x] 任务1.5：`syncer` / `output_runner` 启动路径语义对齐（stage1-next-pass-e）
- **文件**：`syncer/syncer.go`、`syncer/output_runner.go`
- **内容**：
  - 两条路径统一 output 连续失败阈值（300）与致命错误联动退出语义
  - 移除 `InputRunner` 中 checkpoint redis 回退逻辑（checkpointName 配置时必须显式提供 checkpointRedis）

#### [ ] 任务1.4：验收测试
- [ ] Input 单独运行可持续落地
- [ ] Output 单独运行可持续回放
- [x] 单向链路重启不丢进度
- [x] 生命周期分离验证：Output异常退出不影响Input持续落地
- [x] 自恢复验证：Output恢复后可从目标端checkpoint继续回放

**完成标准**：
- [x] Input 与 Output 代码和运行时解耦
- [x] Input checkpoint 只依赖 `meta.redis`
- [x] 端到端单向同步稳定
- [x] Output故障不触发Input联动退出（满足分离生命周期，带受控红线）
- [ ] 达到磁盘水位/一致性红线时可触发可观测的受控联动策略

#### 阶段1仅剩清单
- [ ] Input 单独运行可持续落地（独立运行态验收）
- [ ] Output 单独运行可持续回放（独立运行态验收）
- [ ] 达到磁盘水位/一致性红线时可触发可观测的受控联动策略

---

### 阶段2：Input 高可用（依赖阶段1）

**目标**：Input 实现 Leader/Follower，保障源端采集连续性。
**前置依赖**：阶段2的 Leader/Follower 高可用能力依赖 `meta.redis`（Sentinel 托管）作为协调与 checkpoint 元数据基座；未配置 `meta.redis` 不进入 HA 验收。

**工作量**：4-6天  
**风险**：中

#### [x] 任务2.1：ReplicaLeader 适配 Redis checkpoint
- **文件**：`syncer/replica.go`
- **进展**：
  - 已注入 `meta.redis` 到 `ReplicaLeader`
  - leader 握手起点已支持与 `meta.redis checkpoint` 对齐（同 runId 取更大 offset）

#### [x] 任务2.2：ReplicaFollower 适配 Redis checkpoint
- **文件**：`syncer/replica.go`
- **进展**：
  - 已注入 `meta.redis` 到 `ReplicaFollower`
  - follower 预同步阶段已支持读取 `meta.redis checkpoint`（只读），接管前对齐起点

#### [x] 任务2.3：checkpoint 同步策略
- Leader 负责写 `meta.redis`
- Follower 只读并在接管后接续写入
- 暂停控制仅作用于 Output；Input 不做暂停操作（避免采集中断）
- **进展**：
  - 现状已满足：Input Leader 持续写 `meta.redis`，Follower 在 replica 链路只读 checkpoint
  - 已完成接管场景验收：leader 故障后 follower 接管为 leader，并以 checkpoint 连续点位恢复

#### [x] 任务2.4：验收测试
- Leader 故障接管
- 接管后偏移连续性
- **进展**：
  - 已完成定向编译校验：`go test ./syncer ./cmd -run Test^$ -count=1`
  - 已完成运行态实测：杀死 leader 实例（18012）后，B 侧接管为全 leader（7004/7005/7006）
  - 接管后 `RedisInput psync` 日志显示 checkpoint 与 local/reply 连续（示例：7004 `checkpoint=1363014 -> reply=1363595`）

**完成标准**：
- [x] Input 侧故障切换可用
- [x] 接管前后 checkpoint 连续

---

### 阶段3：Output 高可用（依赖阶段1）

**目标**：Output 实现 Leader/Follower，保障回放连续性。
**前置依赖**：阶段3的 Leader/Follower 高可用能力依赖 `meta.redis`（Sentinel 托管）提供统一协调基座；同时 Output checkpoint 仍写入目标端 Redis。

**工作量**：8-11天  
**风险**：高

#### [x] 任务3.1：实现 OutputLeader / OutputFollower
- **文件**：`syncer/output_leader.go`、`syncer/output_follower.go`
- **进展（首版）**：
  - 已落地 `OutputLeader` 回放主路径骨架（含连续失败红线）
  - 已落地 `OutputFollower` standby骨架（当前不回放、不写checkpoint）
  - 已接入 `syncer.runLeader/runFollower` 生命周期

#### [x] 任务3.2：Output checkpoint 协调
- 仅 Leader 写 `目标端 Redis checkpoint`
- Follower 接管后通过 CAS/epoch 防止覆盖
- **进展（第一版）**：
  - 已在 Output Leader 启动时申请 `leader_epoch`（目标端 `INCR`）
  - 已在 checkpoint 写入链路增加 Lua CAS/Fencing（旧epoch写入被拒绝）
  - 已在日志增加 epoch/token 可观测信息，便于演练排障
- **进展（第二版）**：
  - `OutputFollower` 已支持接管触发（standby -> active）
  - follower 侧收到 handover 信号后会触发 output takeover，并申请新 epoch

#### [x] 任务3.3：验收测试
- 回放不中断
- 切换后无大范围重复/回退
- **进展（第一轮）**：
  - 已执行 handover 注入与重启收敛演练（单实例环境）
  - 已验证 `leader_epoch` 单调递增（基线 `57` -> 演练后 `67`）
  - 当时受限：测试环境无多实例选主器；该限制已由第二轮双实例演练补齐
- **进展（第二轮）**：
  - 已执行双实例演练（A=leader、B=follower），并对 A 注入全分片 handover
  - 观测到 B 侧完成接管并成为 leader（A 状态清空，B 全分片 `Role=leader, State=run`）
  - 目标端 checkpoint `leader_epoch` 在切换后继续单调前进（`181/171/160 -> 182/172/161`）

**完成标准**：
- [x] Output 故障切换可用
- [x] checkpoint 写入单主语义明确

#### 阶段2/3仅剩清单（状态对齐）
- [x] 阶段2任务2.1：ReplicaLeader 适配 Redis checkpoint
- [x] 阶段2任务2.2：ReplicaFollower 适配 Redis checkpoint
- [x] 阶段2任务2.3：checkpoint 同步策略落地与收敛
- [x] 阶段2任务2.4：验收测试（故障接管与checkpoint连续性）
- 结论：阶段2与阶段3主任务已闭环完成，当前无阶段内遗漏。
- 说明：后续待办主要在阶段4（故障切换与一致性收敛）及阶段5（按 Slot Checkpoint）。

---

### 阶段4：故障切换与一致性收敛（依赖阶段2/3）

**目标**：统一故障语义，明确一致性边界。

**工作量**：5-7天  
**风险**：高

#### 任务4.1：故障注入矩阵
- 进程崩溃
- 网络分区
- Sentinel 切主
- 目标端短暂不可用

#### 任务4.2：一致性策略固化
- at-least-once 边界说明
- checkpoint 更新时机标准化
- 回放幂等策略

#### 任务4.3：验收测试
- 各场景恢复时间
- 数据一致性抽样比对

#### 阶段4可直接执行验收脚本清单（含故障注入命令）

**统一前置**（每轮先执行）：
- 启动双实例：
  - `go run . -cmd sync -conf config/cluster2-runtime-a.yaml`
  - `go run . -cmd sync -conf config/cluster2-runtime-b.yaml`
- 状态确认：
  - `powershell -Command "Invoke-WebRequest -UseBasicParsing http://127.0.0.1:18012/syncer/status | % Content"`
  - `powershell -Command "Invoke-WebRequest -UseBasicParsing http://127.0.0.1:18013/syncer/status | % Content"`
- 记录基线：
  - 记录当前 leader 所在实例、`psync` 日志中的 `local/checkpoint/reply`、目标端 `__leader_epoch`。

**脚本 S4-1：进程崩溃（Input/Output 同进程）**
- 注入命令：
  - `powershell -Command "Get-NetTCPConnection -LocalPort 18012 -State Listen | % { taskkill /PID $_.OwningProcess /F }"`
- 观测命令：
  - `powershell -Command "Start-Sleep -Seconds 8; 'A=' + (try {(Invoke-WebRequest -UseBasicParsing http://127.0.0.1:18012/syncer/status).Content} catch {'DOWN'}); 'B=' + (Invoke-WebRequest -UseBasicParsing http://127.0.0.1:18013/syncer/status).Content"`
  - 在 B 实例日志查：`campaign ... new_role(leader)`、`RedisInput ... psync ... checkpoint(...)`
- 通过标准：
  - 存活实例完成接管；`psync` 中 checkpoint 不回退；恢复后持续推进。

**脚本 S4-2：网络分区（本机防火墙模拟）**
- 注入命令：
  - `powershell -Command "New-NetFirewallRule -DisplayName 'GY-DENY-18012-IN' -Direction Inbound -Action Block -LocalPort 18012 -Protocol TCP"`
  - `powershell -Command "New-NetFirewallRule -DisplayName 'GY-DENY-18012-OUT' -Direction Outbound -Action Block -RemotePort 18012 -Protocol TCP"`
- 恢复命令：
  - `powershell -Command "Remove-NetFirewallRule -DisplayName 'GY-DENY-18012-IN'"`
  - `powershell -Command "Remove-NetFirewallRule -DisplayName 'GY-DENY-18012-OUT'"`
- 观测重点：
  - 分区期间 follower 出现 `Unavailable/connection error`，随后重新选主并收敛。
- 通过标准：
  - 分区解除后角色稳定；checkpoint 连续；无长时间双主。

**脚本 S4-3：Sentinel 切主（meta.redis）**
- 注入命令：
  - `go run ./tools/redis_admin -addr 127.0.0.1:26379 -cmd SENTINEL -a1 failover -a2 mymaster`
  - 说明：当前本地 Sentinel 未启用 `requirepass`，不传 `-pass`；若启用鉴权再补 `-pass/-user`。
- 观测命令：
  - 日志查：`sentinel discovered master`、`sentinel connect success`
  - 运行态查：`/syncer/status` 持续 `run`
- 通过标准：
  - 切主后 checkpoint 读写不中断；实例不因 sentinel 切主退出。

**脚本 S4-4：目标端短暂不可用（仅 Output 压测）**
- 注入命令（示例阻断目标集群端口）：
  - `powershell -Command "1..6 | % { New-NetFirewallRule -DisplayName ('GY-DENY-800' + $_) -Direction Outbound -Action Block -RemotePort (8000 + $_) -Protocol TCP }"`
- 恢复命令：
  - `powershell -Command "1..6 | % { Remove-NetFirewallRule -DisplayName ('GY-DENY-800' + $_) }"`
- 观测重点：
  - Output 出现重试日志；Input 仍持续落地；恢复后 Output 从 checkpoint 继续回放。
- 通过标准：
  - 无不可恢复退出；恢复后延迟收敛，checkpoint 单调推进。

**脚本 S4-5：一致性抽样比对（最小闭环）**
- 样本写入（源端）：
  - `go run ./tools/redis_admin -cluster -addr 127.0.0.1:7001 -pass EPBQMMNnprXsXCZ6wsnZ -cmd SET -a1 stage4:key:1 -a2 v1`
  - `go run ./tools/redis_admin -cluster -addr 127.0.0.1:7001 -pass EPBQMMNnprXsXCZ6wsnZ -cmd HSET -a1 stage4:hash:1 -a2 f1 -a3 v1`
- 目标抽样：
  - `go run ./tools/redis_admin -cluster -addr 127.0.0.1:8001 -pass EPBQMMNnprXsXCZ6wsnZ -cmd GET -a1 stage4:key:1`
  - `go run ./tools/redis_admin -cluster -addr 127.0.0.1:8001 -pass EPBQMMNnprXsXCZ6wsnZ -cmd HGET -a1 stage4:hash:1 -a2 f1`
- 专项说明（7006/high 分片）：
  - 验证高分片时，样本 key 必须先确认 `slot ∈ [10923,16383]`，避免“写入命中其他分片”造成误判。
- 通过标准：
  - 故障前后抽样值一致；允许 at-least-once 下短窗口重复写，但不允许回退丢失。

**脚本 S4-6：源端/目标端 Redis 切换不影响同步（新增）**
- 目标：
  - 源端 Redis 拓扑切换（主从切换/slot owner 变更）后，Input 可自动续传，不中断长期同步；
  - 目标端 Redis 拓扑切换后，Output 可自动恢复写入，不出现不可恢复中断。
- 注入命令（示例）：
  - 源端切换：对源端集群对应主节点执行 `CLUSTER FAILOVER`（或等价主从切换操作）
  - 目标端切换：对目标端集群对应主节点执行 `CLUSTER FAILOVER`（或等价主从切换操作）
  - 若使用 Sentinel 托管 standalone，执行 `SENTINEL failover <masterName>`
- 观测重点：
  - 源端切换后：`RedisInput psync`/拓扑刷新日志连续，无长时间停滞，`input_cp(meta.redis)` 单调；
  - 目标端切换后：Output 重连/重试后恢复发送，`output_cp(目标端)` 单调，无不可恢复错误；
  - 切换前后：抽样 key 最终收敛，允许短窗口 at-least-once 重放，不允许长期缺失。
- 通过标准：
  - 源端切换与目标端切换分别通过；同步任务保持 `run`；
  - checkpoint 不回退（允许短窗口重复写但需可观测）；
  - 60s~120s 观测窗口内抽样一致性恢复。

**验收记录模板（每轮必填）**：
- 场景编号、注入时间、恢复时间、RTO、是否出现双主、checkpoint 是否单调、抽样一致性结果、异常日志摘要、是否涉及源端切换/目标端切换。

#### 阶段4第一轮实测记录（已执行）
- `S4-1` 进程崩溃：
  - 注入：终止 `18013`（当时 leader 实例）
  - 结果：`18012` 接管为全 leader（7004/7005/7006）
  - RTO：约 `10637ms`
  - 证据：`campaign ... new_role(leader)`；`RedisInput psync` 中 `checkpoint/local/reply` 连续（无回退）
- `S4-3` Sentinel 切主：
  - 注入：`SENTINEL failover mymaster`
  - 结果：实例保持 `run`，日志持续出现 `sentinel discovered master` / `sentinel connect success`
  - 备注：本地 Sentinel 无鉴权，首次携带 `-pass` 会报 `ERR Client sent AUTH, but no password is set`，改为无密码命令后通过
- `S4-2` 网络分区（第一轮）：
  - 注入：按脚本阻断 `18012` 入/出方向端口（本机双实例同宿主）
  - 结果：本轮未触发角色变化（A 持续 leader、B 持续 follower），判定为“注入未生效到选主链路”
  - 原因判断：本机回环流量与当前选主/续租路径未被该规则有效隔离（该轮结果不计入通过）
  - 下一轮修正：改为阻断单实例到协调后端（Redis 700x/Sentinel 2637x）的出站连通，或改跨主机演练
- `S4-2` 网络分区（第二轮）：
  - 注入：按 leader 进程路径定向阻断到 `7001-7006/26379-26381/8001-8006` 出站连通
  - 结果：仍未触发角色变化（`TAKEOVER=NO`，A 持续 leader、B 持续 follower）
  - 结论：单机同宿主条件下，当前防火墙注入无法稳定命中“选主链路分区”语义；该项需跨主机或网络设备级隔离继续验证
- `S4-4` 目标端短暂不可用（第一轮）：
  - 注入：阻断目标端端口 `8001-8006` 出站（约16s），随后恢复
  - 结果：实例持续 `run`；Input 未中断；Output 在注入窗口出现 checkpoint 停滞，恢复后继续单调推进
  - 证据：`RedisOutput set checkpoint` 在注入期多次重复同 offset，恢复后 offset 继续增长（如 `7005: 200402 -> 200743 -> 201084 -> 201534 -> 201875 -> 202325 -> 202666`）
  - 结论：符合“目标端短暂不可用时，Output重试且恢复后可继续回放”的预期
- `S4-5` 一致性抽样比对（第一轮）：
  - 方法：源端批量写入 `SET/HSET` 并覆盖到最终值（`v2-*`），目标端 60s 轮询收敛
  - 结果：未在 60s 内全部收敛（`S4-5 FAIL`）
  - 复查：30s 后仍存在部分键缺失（示例：`stage4:s5:diag:key:3`、`stage4:s5:diag:hash:1` 在目标端 `nil`）
  - 结论：一致性抽样未通过，需进入问题定位（优先排查 Output 过滤/回放链路对普通 key/hash 的遗漏条件）
- `S4-5` 定位专项（第二轮进展）：
  - 已定位“按 slot 分片写入”表现：`mid/high` 可到达目标，`low(0-5460)` 持续缺失
  - 证据：trace 命令仅见 `mid/high` 被 `Print cmd to target` 打印，`low` 未进入 output 发送链路
  - 进一步证据：`7004` 分片 `RedisOutput stats` 持续 `sendCmd(0)`，而 `7005/7006` 有发送
  - 排除项：源端 `7004` 从节点（READONLY）可读到 `low` 键，说明不是源端复制未到位
  - 当前判断：问题集中在 `7004(low slot)` 这条 Input->Output 回放链路（非全链路故障）
- `S4-5` 正式复验（第三轮，cluster脚本修正后）：
  - 方法：使用 `tools/redis_admin -cluster`/cluster client 按 slot 定向写入 `low/mid/high` 三类样本键，并校验 `CLUSTER KEYSLOT` 命中区间
  - 结果：`S4-5 PASS`（45s 内 `low/mid/high` 均收敛到目标端）
  - 样本：`low(slot=3799)`、`mid(slot=6444)`、`high(slot=13941)`，目标端读取值与源端一致
- `S4-4` 干净重验（第二轮，单实例B观测）：
  - 前置：仅保留 `18013(B)` 运行，`/syncer/status` 显示 `7004/7005/7006` 全 leader/run
  - 注入：进程级停掉目标端 `8001-8006`，16s 后全部拉起恢复
  - 过程：注入窗口内 `B` 实例持续 `run`（生命周期未退出）
  - 结果：恢复后 `mid/high` 收敛，`low` 仍未收敛（`S4-4 FAIL`）
  - 样本：`low(slot=2996)->nil`，`mid(slot=7247)->OK`，`high(slot=11606)->OK`
  - 当前判断：`S4-4` 仍存在 `low slot` 特定链路问题，需继续专项定位（重点看 `7004` Output 实际发送路径）
- `S4 low-slot followup`（最小化定点打点 + 语义校正）：
  - 打点范围：仅 `7004` 链路 + `stage4:lowprobe:*` 样本
  - 观测证据：已确认 `AofWriter(7004)` 落地、`RedisOutput(7004)` 入队与发送（`enqueue sendBuf` / `Print cmd to target`）
  - 语义约束：双向同步回环过滤场景下，事务内任一命令命中 `FilterCheckpointKey` 时，必须整包事务丢弃，防止回环标记与业务命令混放导致回流
  - 修正：撤销“仅跳过事务内单条命中命令”的变更，恢复为 `shouldIgnore=true` 后在 `exec` 丢弃整包事务
  - 复验：重跑目标端停启后，`low/mid/high` 三段样本均在 45s 内收敛（`S4-4 RECHECK PASS`）
  - 收尾：专项临时打点已清理（`output/aof_writer`），仅保留回环过滤语义一致的实现
- `S4-6` 源端/目标端 Redis 切换验证（第一轮/第二轮）：
  - 第一轮（源端/目标端各分片同时切换）：`FAIL`
  - 第二轮（源端/目标端各单分片切换）：`FAIL`
  - 现象：切换后目标端样本键 `stage4:s6:key:low` 在 120s 内未收敛（持续空值）；运行态由 `run` 退化为 `stop`
  - 关键日志：`renew error ... move ... getNodeByAddr ... not found`、`checkSyncDelay set testkey ... error(move ...)`、`syncer is stopped`
  - 当前判断：源端拓扑切换触发了节点地址映射/续租链路的 MOVED 处理缺陷，导致实例进入停止态；`S4-6` 暂不通过，需专项修复后复测
- `S4-6` 修复后复测（第三轮）：
  - 修复点：`cluster` 客户端在处理 `MOVED/ASK` 时，`getNodeByAddr` miss 增加兜底解析（端口唯一匹配 + 临时节点直连），并在临时节点命中后触发拓扑刷新
  - 注入：源端单分片切换 + 目标端单分片切换（脚本 `scripts/ha_s4_6.ps1`）
  - 结果：`PASS`（切换期间任务保持 `run`，样本键最终从 `v0/v1` 收敛到 `v2`）
  - 结论：当前环境下“源端/目标端 Redis 切换不影响同步”已通过

**完成标准**：
- [~] 故障场景可恢复（S4-1/S4-3/S4-4/S4-6 已通过；S4-2 在单机环境注入无效，待跨主机重测）
- [x] 一致性边界有文档并通过验证（S4-5 正式复验通过，low-slot专项已定位修复并复测通过）

---

### 阶段5：验证双向同步（依赖阶段0-4）

**目标**：验证 A->B 与 B->A 长稳运行、回环过滤有效、双向切换/重启后可持续收敛。

**工作量**：3-5天  
**风险**：中-高

#### [x] 任务5.1：双向链路对开验证
- **内容**：
  - 同时启动 A->B、B->A 双任务
  - 持续写入冲突敏感样本（string/hash/zset + 事务）
  - 观察是否出现异常回流放大

#### [x] 任务5.2：回环过滤有效性验证
- **内容**：
  - 针对 `FilterCheckpointKey` 与业务事务混合场景验证“整包事务丢弃”语义
  - 校验双向链路下不过滤业务、不过度放行回环标记

#### [x] 任务5.3：双向切换/重启验证
- **内容**：
  - 源端切换、目标端切换、进程重启组合注入
  - 校验 `input_cp(meta.redis)` 与 `output_cp(目标端)` 连续性
  - 增加 `meta.redis(Sentinel)` 切主期间协调续租演练：
    - 注入：`SENTINEL failover mymaster`
    - 观测：`cluster renew/campaign` 错误窗口、`/syncer/status` 是否保持 `run`、是否触发进程退出
    - 验收：允许短时重试日志，不允许长期 `stop` 或不可恢复中断

#### [x] 任务5.4：延迟度量改造（替换 syncDelayTestKey）
- **内容**：
  - 取消 `syncDelayTestKey: redis-GunYu-syncDelay-testKey` 参数与注入式测量路径
  - 改为基于双 checkpoint 对比计算延迟：
    - `input_cp(meta.redis)` 作为采集进度基线（offset/mtime）
    - `output_cp(目标端)` 作为回放进度（offset/mtime）
  - 产出并暴露两个核心指标：
    - `lag_offset = input_cp.offset - output_cp.offset`
    - `lag_time = input_cp.mtime - output_cp.mtime`（或等价时间差）
  - 增加异常判定：checkpoint 不连续/倒退时打告警并标记度量无效窗口
  - 指标实现口径（方式2，默认 `meta.redis` 与目标端时钟一致）：
    - `redis_gunyu_output_checkpoint_lag_offset{input=...}`
    - `redis_gunyu_output_checkpoint_lag_time{input=...}`（单位：ns）
    - `redis_gunyu_output_checkpoint_lag_valid{input=...}`（1=有效，0=无效）
  - 有效门控（任一命中即 `lag_valid=0`，并将 lag 指标置为 `-1`）：
    - runId 为空/非法，或 `input_cp.runid != output_cp.runid`
    - 任一侧 checkpoint 不存在、读失败、字段非法（offset<0 或 mtime<=0）
    - 计算结果为负值（`lag_offset<0` 或 `lag_time<0`）
    - 同一 runId 下 checkpoint offset 回退（判定为无效窗口）
  - 告警建议（仅在 `lag_valid=1` 时生效）：
    - P3：`lag_offset > 10_000` 持续 1 分钟
    - P2：`lag_offset > 100_000` 或 `lag_time > 30s` 持续 3 分钟
    - P1：`lag_offset > 1_000_000` 或 `lag_time > 120s` 持续 5 分钟
  - Prometheus 告警规则样例（可直接落地，含 `lag_valid` 门控）：
```yaml
groups:
  - name: redis-gunyu-lag
    rules:
      - alert: RedisGunYuLagP3
        expr: |
          redis_gunyu_output_checkpoint_lag_valid == 1
          and redis_gunyu_output_checkpoint_lag_offset > 10000
        for: 1m
        labels:
          severity: P3
        annotations:
          summary: "Redis-GunYu lag P3 on {{ $labels.input }}"
          description: "checkpoint lag_offset > 10000 for 1m"

      - alert: RedisGunYuLagP2
        expr: |
          redis_gunyu_output_checkpoint_lag_valid == 1
          and (
            redis_gunyu_output_checkpoint_lag_offset > 100000
            or redis_gunyu_output_checkpoint_lag_time > 30000000000
          )
        for: 3m
        labels:
          severity: P2
        annotations:
          summary: "Redis-GunYu lag P2 on {{ $labels.input }}"
          description: "lag_offset > 100000 or lag_time > 30s for 3m"

      - alert: RedisGunYuLagP1
        expr: |
          redis_gunyu_output_checkpoint_lag_valid == 1
          and (
            redis_gunyu_output_checkpoint_lag_offset > 1000000
            or redis_gunyu_output_checkpoint_lag_time > 120000000000
          )
        for: 5m
        labels:
          severity: P1
        annotations:
          summary: "Redis-GunYu lag P1 on {{ $labels.input }}"
          description: "lag_offset > 1000000 or lag_time > 120s for 5m"

      - alert: RedisGunYuLagInvalidWindow
        expr: redis_gunyu_output_checkpoint_lag_valid == 0
        for: 2m
        labels:
          severity: P2
        annotations:
          summary: "Redis-GunYu lag metric invalid on {{ $labels.input }}"
          description: "checkpoint lag is in invalid window for more than 2m"
```

#### [ ] 任务5.5：验收测试
- 双向 24h 稳态回放无异常放大
- 抽样对账通过
- 异常场景恢复后可追平
- checkpoint 对比延迟指标稳定、可观测（lag_offset/lag_time）
- `meta.redis(Sentinel)` 切主注入后，协调续租可恢复（无长期 `renew error` 导致的不可恢复退出）

#### [~] 任务5.6：差异风险专项验证（冲突/非幂等/TTL/Lua）
- **内容**：
  - 冲突写验证：A/B 同时写同 key，观测最终值与覆盖顺序，明确“最后写入生效”边界。
  - 非幂等命令验证：`INCR/HINCRBY/ZINCRBY/XADD` 在重试/切换窗口内压测，量化重复回放影响。
  - TTL 验证：同时对比 value + ttl，确认延迟窗口下过期漂移范围与告警阈值。
  - Lua 验证：区分确定性脚本与非确定性脚本，确认双向下可重放一致性边界。
  - 产出专项脚本与报告：`scripts/stage5_risk_verify.sh`（命令级），固定采样与记录模板。

**通过标准（任务5.6）**：
- [x] 已明确并文档化“冲突写最终一致性”边界与业务约束
- [x] 已量化非幂等命令在故障窗口的偏差范围
- [~] 已给出 TTL/Lua 两类差异场景的验收口径与告警阈值（TTL已补齐，Lua待补测）
- [x] 已形成可复用的一键验证脚本与结果记录模板

#### 阶段5补充记录（2026-03-05，双向专项排查与修复）
- **问题现象**：
  - 双向配置测试中，目标端出现了不应回流的 `redis-GunYu-Checkpoint-ClusterB*` 键。
  - 运行日志伴随大量 `slot-key(... ) not start with checkpoint(...)` 前缀不一致告警。
- **根因定位**：
  - 反向链路配置方向错误：`cluster2-runtime-b.yaml` 与 `cluster2-runtime-a.yaml` 同向（均为 `A->B`），未形成真实 `B->A`。
  - slot-key 文件共享导致污染：两条链路共用 `slot-key.txt`，不同 checkpoint 前缀相互覆盖。
  - 非事务脚本命令漏过滤：`EVAL` 场景中可能携带 `FilterCheckpointKey`，未被事务分支拦截。
- **修复项**：
  - 配置修正：`config/cluster2-runtime-b.yaml` 改为 `input=800x`、`output=700x`，并切换为
    `syncCheckPointKey=redis-GunYu-Checkpoint-ClusterB`、
    `filterCheckPointKey=redis-GunYu-Checkpoint-ClusterA`。
  - slot-key 隔离：`main.go` 将 cluster slot 映射文件改为按 checkpoint 前缀命名
    （`slot-key-<syncCheckPointKey>.txt`），避免双向任务互相污染。
  - 回环过滤兜底：`syncer/output.go` 在非事务路径增加基于原始参数载荷的
    `FilterCheckpointKey` 检测，命中即丢弃（覆盖 `EVAL`/脚本写 checkpoint 场景）。
  - 回归测试：`syncer/output_parse_test.go` 新增 `EVAL` 命中过滤键的丢弃用例。
- **复测结论**：
  - 在“清空 A/B 集群 + 清空 metaRedis + 仅启动 A->B 与 B->A 两链路”条件下复测通过。
  - 双向方向正确：A->B 与 B->A 均可正常运行，且不再出现 slot-key 前缀污染告警。
  - 回环过滤符合预期：B 侧不再出现 `ClusterB*` 回流键，A 侧不再出现 `ClusterA*` 回流键。

#### 阶段5补充记录（2026-03-05，restore 冲突与防呆开关）
- **问题现象**：
  - 双向运行中出现 `BUSYKEY Target key name already exists`，随后 output 回放报错重试，导致部分命令未稳定收敛。
  - 异常窗口内可见基础样本不一致与非幂等命令放大（如 `HINCRBY/ZINCRBY` 偏大）。
- **根因定位**：
  - 双向场景下 `restore` 命令回放在目标端命中已存在 key，触发 `BUSYKEY`，进而影响后续 AOF 回放稳定性。
- **修复项**：
  - 新增配置开关 `output.replay.allowRestoreReplay`（默认 `false`）。
  - Output 初始化增加防呆：默认将 `restore` 加入命令黑名单；仅在 `allowRestoreReplay=true` 时放开。
  - 运行配置从“手工 commandBlacklist: restore”切换为统一开关语义（`allowRestoreReplay: false`）。
- **复测结论**：
  - 在“停进程 + 清空 A/B/meta + 清理本地 channel 落地目录 + 重启双向”条件下复测：
    - 基础双向样本 `SET/HSET/ZADD` 收敛正常；
    - 日志中未再出现 `BUSYKEY` 与 `reply is not ok` 同类错误。
  - 新增“双写同 key”冲突验证：两侧最终值可收敛到同一值，但结果由写入时序决定，不保证业务可预测一致。

#### 阶段5补充记录（2026-03-05，非幂等+故障切换（非双写）复测）
- **场景与注入**：
  - 双向链路运行（A->B、B->A），仅在 A 侧单向写入（非双写）。
  - 命令样本：`INCR/HINCRBY/ZINCRBY` 持续写入。
  - 故障注入：源端单分片 `CLUSTER FAILOVER TAKEOVER`、目标端单分片 `CLUSTER FAILOVER TAKEOVER`、A->B 同步进程重启。
- **复测结果**：
  - `HINCRBY/ZINCRBY`：A/B 最终值一致（本轮样本为 260/260）。
  - `INCR`：A 侧为 260，B 侧为空值（`nil`），本轮未收敛。
  - 结论：当前“非幂等+故障切换（非双写）”场景**尚未完全通过**，需针对 `INCR` 路径继续定位（分片切换窗口下的回放/续传链路）。

#### 阶段5补充记录（2026-03-05，INCR 链路专项定位与最小修复）
- **根因定位**：
  - 异常 key `stage5:fault:incr` 位于低分片（`slot=4476`），与故障注入分片重合，问题表现为该分片切换窗口后增量续传不连续。
  - `syncer/input.go` 的 `syncMeta` 在增量同步分支会将 `psync` 返回的实际 `runId` 强制改写为 `id1`，在 failover 后 `replid2 -> replid1` 过渡窗口可能造成 `runId/offset` 语义错位，影响分片续传。
- **最小修复**：
  - 移除增量分支中的 `sOffset.RunId = id1` 强制改写逻辑，保留 `psync` 实际返回的 `runId`。
  - 变更文件：`syncer/input.go`（最小补丁，单点修改）。
- **修复后复测**：
  - 编译校验：`go build .` 通过。
  - 定点回归（低分片 `INCR`）：`A=263, B=263`，恢复收敛。
  - 注入回归（源/目标分片切换）：`A=20, B=20`，保持一致。
  - 注入回归（A->B 进程重启后）：`A=30, B=30`，保持一致。
  - 结论：`INCR` 在“非幂等 + 故障切换（非双写）”路径已恢复通过。

**完成标准**：
- [~] 双向同步稳定运行（关键场景已通过，24h稳态待补）
- [x] 回环过滤语义正确（无明显误过滤/漏过滤）
- [x] 双向切换与重启后可持续收敛
- [x] 延迟指标由 checkpoint 对比给出且可用于告警

---

### 阶段6：按 Slot Checkpoint（可选增强，依赖阶段0-5）

**目标**：支持不同分片拓扑双向同步。

**工作量**：9-13天  
**风险**：中

#### 任务6.1：Redis checkpoint 结构升级
- **文件**：`pkg/redis/checkpoint/checkpoint.go`
- **内容**：
  - 每 slot 独立 checkpoint 字段（0-16383）
  - 提供批量读写接口

#### 任务6.2：Output 按 slot 更新 checkpoint
- **文件**：`syncer/output.go`

#### 任务6.3：Input 按 slot 聚合起点
- **文件**：`syncer/input.go`

#### 任务6.4：性能优化
- 仅更新变更 slot
- 合并网络往返

#### 任务6.5：验收测试
- 异构分片双向同步验证

**完成标准**：
- [ ] slot 级 checkpoint 完整
- [ ] 异构分片双向同步可用

---

### 阶段7：前端 Web 系统管理平台（可选增强，依赖阶段0-6）

**目标**：提供可视化管理能力，覆盖同步程序安装配置、任务生命周期管理、多实例统一纳管与已有实例接入。

**工作量**：10-14天  
**风险**：中-高

#### 任务7.1：平台基础架构与鉴权
- **文件**：`web/`（新增）、`api/`（新增或复用）
- **内容**：
  - 建立前后端基础框架（登录、会话、权限模型）
  - 提供任务列表、实例列表、节点状态总览
  - 对接现有 `/syncer/status` 与核心指标采集接口

#### 任务7.2：同步程序安装与配置向导
- **内容**：
  - 提供“新建同步任务”向导：源端/目标端/meta.redis 参数配置与连通性校验
  - 支持生成/下发配置模板（含 `syncCheckPointKey`、`filterCheckPointKey` 规则校验）
  - 支持启动/停止/重启任务与失败原因可视化

#### 任务7.3：多套同步程序统一管理
- **内容**：
  - 支持按环境/集群/业务分组管理多套同步程序
  - 支持批量操作（批量发布配置、批量启停、批量巡检）
  - 提供运行态看板（lag、checkpoint、错误率、重试次数）

#### 任务7.4：已有同步程序纳管
- **内容**：
  - 支持导入已有运行实例（IP/端口/配置指纹）
  - 建立“只读接入 -> 接管控制”两阶段纳管流程
  - 纳管后支持一致的日志、告警、审计视图

#### 任务7.5：验收测试
- 新建任务从安装到运行全流程可闭环
- 同时管理多套同步程序无明显性能退化
- 既有实例纳管成功且不影响现网运行
- 关键操作（配置变更、启停、接管）有审计记录

**完成标准**：
- [ ] Web 平台可完成同步任务安装配置与生命周期管理
- [ ] 支持多套同步程序统一纳管与批量运维
- [ ] 支持已有同步程序平滑纳管（先观测后接管）
- [ ] 核心运行指标与告警在平台可观测

---

## 四、当前推荐执行顺序

1. **立刻开始阶段0**（元数据 Redis Sentinel + checkpoint 读写）
2. 阶段0稳定后，推进阶段1（解耦）
3. 再并行推进阶段2/3（Input/Output HA）
4. 阶段4收敛一致性
5. 阶段5完成双向同步验证
6. 阶段6作为增强（Slot Checkpoint）
7. 阶段7作为增强（Web 管理平台）

---

## 五、工期总览（重排后）

| 阶段 | 工作量 | 风险 |
|---|---|---|
| 阶段0：元数据Redis Sentinel + checkpoint | 4-6天 | 中-高 |
| 阶段1：Input/Output解耦 | 6-9天 | 中 |
| 阶段2：Input高可用 | 4-6天 | 中 |
| 阶段3：Output高可用 | 8-11天 | 高 |
| 阶段4：故障切换与一致性 | 5-7天 | 高 |
| 阶段5：双向同步验证 | 3-5天 | 中-高 |
| 阶段6：Slot checkpoint（可选） | 9-13天 | 中 |
| 阶段7：Web管理平台（可选） | 10-14天 | 中-高 |

- 必选总工期（阶段0-5）：**30-44天**
- 含阶段7总工期（不含阶段6）：**40-58天**
- 含阶段6+7总工期：**49-71天**

---

## 六、验收口径（关键）

- `input_cp` 真相源是 `meta.redis`（Sentinel 托管）
- `output_cp` 真相源是目标端 Redis（用于回放续传与回环过滤边界）
- 任意单点进程重启后可按 checkpoint 续传
- 双向同步下回环过滤策略与 checkpoint 策略一致

---

## 七、备注

- 历史“文件 checkpoint”方案不再作为主路径，仅保留回滚参考。
- 进入阶段1之前，阶段0必须在测试环境完成切主演练并通过。

---

## 八、工业级验收门槛

### 8.1 SLO 与恢复目标

- [ ] **可用性目标**：同步服务月度可用性 >= 99.9%
- [ ] **恢复时间目标（RTO）**：单节点故障后 30s 内恢复同步
- [ ] **数据丢失目标（RPO）**：进程异常重启场景 RPO <= 1s（按checkpoint刷新周期）
- [ ] **重复回放窗口**：故障切换后允许小窗口 at-least-once，窗口上限可观测且可告警

### 8.2 故障演练门槛（必须通过）

- [ ] **Sentinel切主演练**：主从切换后自动恢复 checkpoint 读写与同步
- [ ] **meta.redis协调续租演练**：Sentinel 切主期间允许短时续租报错，但任务需自动恢复并保持可用
- [ ] **网络分区演练**：短时网络抖动/分区后无长时间卡死
- [ ] **目标端不可用演练**：目标Redis不可用后可重试并恢复，不出现不可恢复中断
- [ ] **进程反复重启演练**：连续重启后仍可按 checkpoint 续传
- [ ] **分离生命周期演练**：仅Output故障时，Input保持持续落地且不退出
- [ ] **恢复追平演练**：Output恢复后可自动追平积压并持续更新目标端checkpoint

### 8.3 数据一致性门槛

- [ ] **双向回环控制**：过滤策略稳定，无明显误过滤/漏过滤
- [ ] **偏移单调性**：checkpoint offset 单调递增（允许故障恢复回退窗口有记录）
- [ ] **抽样对账**：按库/slot抽样对账通过率 >= 99.99%
- [ ] **一致性报告**：每次发布后产出一致性验证报告

### 8.4 可观测性与告警门槛

- [ ] **核心指标齐全**：lag、checkpoint延迟、回放QPS、失败率、重试次数、切主耗时
- [ ] **告警分级**：P1/P2/P3 告警阈值、抑制与升级链路明确
- [ ] **日志可追踪**：按 taskId/runId/slot 可关联输入、回放、checkpoint 三段日志
- [ ] **审计可回放**：关键操作（切主、重试、回退）有审计记录

### 8.5 发布与回滚门槛

- [ ] **灰度发布能力**：支持按任务/实例灰度，支持快速回退
- [ ] **双栈兼容窗口**：etcd 与 meta.redis 迁移期间可灰度并行验证
- [ ] **回滚手册**：故障场景有标准回滚SOP，值班人员可执行
- [ ] **变更冻结机制**：重大活动期间具备冻结与快速止损机制

### 8.6 安全与合规门槛

- [ ] **访问控制**：元数据Redis启用ACL最小权限
- [ ] **传输安全**：生产环境支持TLS（按环境可配置）
- [ ] **密钥管理**：账号密码轮换机制与泄露应急预案
- [ ] **多租户隔离**：不同环境/任务 key namespace 强隔离

### 8.7 双Checkpoint边界与隐患防护（必须明确）

- [ ] **命名与职责隔离**：`input_cp` 仅记录源端采集进度（存放于 `meta.redis`），`output_cp` 仅记录回放进度（存放于目标端）；两者使用不同前缀/命名空间，禁止复用key。
- [ ] **ACK后前移规则**：`output_cp` 仅在目标端写入成功并收到确认后更新，禁止“先更新checkpoint后写数据”。
- [ ] **回环过滤隔离**：A->B 与 B->A 使用独立过滤标记key，避免checkpoint相关命令被反向链路误处理。
- [ ] **落地文件保留保护**：AOF/RDB 清理策略必须受 `output lag` 保护，确保“checkpoint在、文件也在”，避免读取断档。
- [ ] **拓扑变更连续性**：源端/目标端拓扑变化后，runId/replid 与 checkpoint 键选择规则保持连续，支持自动恢复与重试。
- [ ] **降级与兜底策略**：当 `meta.redis` 或目标端checkpoint读取失败时，必须有可观测的降级策略（重试、回退安全点、告警），避免静默错位。

> 注：按当前设计，`input_cp(meta.redis)` 与 `output_cp(目标端)` 在命名隔离正确的前提下不会互相覆盖；主要风险不在“覆盖”，而在“语义混淆、更新时机、回环与清理策略”。
